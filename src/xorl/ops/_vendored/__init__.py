"""Vendored third-party trees (issue #78 phase 1).

Every subtree here is a snapshot of external code with its own
``VENDORED.md`` (provenance, upstream pin, local-patch ledger).  Nothing in
this namespace is linted, formatted, or hand-edited; see
``src/xorl/ops/README.md`` for the edit policy.
"""
